type User {
  id: String!
  email: String!
  # name: String
  posts: [Post]
  # comments: [Comment]
}

type Post {
  id: String!
  title: String!
  # content: String
  author: User
  # comments: [Comment]
}

# type Comment {
#   id: String!
#   content: String!
#   author: User
#   post: Post
# }

type Query {
  users: [User]
  user(id: String!): User
  # posts: [Post]
  post(id: String!): Post!
  # comments: [Comment]
  # comment(id: String): Comment!
}

type Mutation {
  addUser(email: String): User
  addPost(title: String!, authorId: String): Post
  # addComment(email: String): User
  # addUser(email: String): User
}
