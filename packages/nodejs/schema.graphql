type User {
  id: String!
  email: String!
  name: String
  posts: [Post]
  comments: [Comment]
}

type Post {
  id: String!
  title: String
  content: String
  author: User
  comments: [Comment]
}

type Comment {
  id: String!
  content: String
  author: User
  post: Post
}
